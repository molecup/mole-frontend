import Container from '@mui/material/Container';
import Typography from '@mui/material/Typography';

export default function Home(){
    return(
        <>
            <Container>
                <Typography variant="h2">Pagina principale</Typography>
                Questa sarà la pagina di presentazione del progetto (tipo le slides)
            </Container>
        </>
    );
}